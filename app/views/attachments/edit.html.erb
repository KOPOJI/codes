<% content_for :title, "#{t 'Image editing'} ##{@attachment.id}" %>
<% title "#{t 'Image editing'} ##{@attachment.id}" %>
<% description "#{t 'Image editing'} ##{@attachment.id}" %>
<% keywords t 'Image editing' %>

<%= render 'form' %>

<%= link_to t('Show'), @attachment, class: 'link' %> |
<%= link_to t('Back'), attachments_path, class: 'link' %>
